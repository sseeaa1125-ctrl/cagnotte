---
phase: 07-banani-delta-polish
plan: 01
type: execute
wave: 1
depends_on: [06-02]
files_modified:
  - src/lib/constants.ts
  - src/components/layout/PublicNavbar.tsx
  - src/components/layout/DashboardNavbar.tsx
  - src/components/cagnottes/CampaignCard.tsx
  - src/app/(authed)/tableau-de-bord/page.tsx
  - src/app/(authed)/tableau-de-bord/cagnottes/[slug]/page.tsx
autonomous: true
requirements: [PLSH-01, PLSH-02, PLSH-03]
must_haves:
  truths:
    - "Both navbars render the lowercase two-tone cagnotte.sn logo (.sn in text-gray-400, font-black tracking-tighter)."
    - "Clicking a cagnotte card on /tableau-de-bord opens /tableau-de-bord/cagnottes/[slug] (creator detail), NOT /c/[slug]."
    - "/tableau-de-bord/cagnottes/[slug] is server-owner-gated (404 for non-owner) and renders the DashboardCagnotteDetail layout (header, KPI grid, Withdraw Action Box, recent participations, sidebar with share/visibility/danger zone)."
    - "CampaignCard preserves existing public behavior — linkVariant defaults to 'public' so all current public call-sites are unchanged."
  artifacts:
    - path: "src/lib/constants.ts"
      provides: "MISC.brandMark/brandSuffix or siteName updated; CREATOR_DETAIL_LABELS block added"
      contains: "brandMark"
    - path: "src/components/layout/PublicNavbar.tsx"
      provides: "Two-span lowercase cagnotte.sn logo"
      contains: "text-gray-400"
    - path: "src/components/layout/DashboardNavbar.tsx"
      provides: "Two-span lowercase cagnotte.sn logo"
      contains: "text-gray-400"
    - path: "src/components/cagnottes/CampaignCard.tsx"
      provides: "linkVariant prop defaulting to 'public'"
      contains: "linkVariant"
    - path: "src/app/(authed)/tableau-de-bord/cagnottes/[slug]/page.tsx"
      provides: "Creator cagnotte detail (server component, owner-gated)"
      min_lines: 80
  key_links:
    - from: "src/app/(authed)/tableau-de-bord/page.tsx"
      to: "src/components/cagnottes/CampaignCard.tsx"
      via: "linkVariant=\"creator\" prop on dashboard CampaignCard usage"
      pattern: "linkVariant=\"creator\""
    - from: "src/app/(authed)/tableau-de-bord/cagnottes/[slug]/page.tsx"
      to: "/api/cagnottes/:slug"
      via: "server fetch (cookie-forwarded)"
      pattern: "api/cagnottes"
    - from: "src/app/(authed)/tableau-de-bord/cagnottes/[slug]/page.tsx"
      to: "/api/withdrawals/balance"
      via: "server fetch for netWithdrawable"
      pattern: "api/withdrawals/balance"
---

<objective>
Phase 7, Plan 01 — P0 gap fixes: creator cagnotte detail page + CampaignCard link bug + lowercase two-tone logo swap.

Purpose: Close the most user-visible gaps from the manual QA against Banani. Currently the dashboard CampaignCard sends owners to the public donor page, the navbar logo is wrong, and there is no creator-side cagnotte detail page at all. This plan delivers all three fixes atomically.

Output:
- New route `/tableau-de-bord/cagnottes/[slug]` rendering the Banani DashboardCagnotteDetail layout (server component, server-side owner check via JWT seller id).
- `CampaignCard` gains a `linkVariant?: "public" | "creator"` prop (default `"public"` to preserve every existing call-site).
- `PublicNavbar` + `DashboardNavbar` render the verbatim two-span lowercase `cagnotte<span>.sn</span>` logo.
- `MISC.siteName` (or `MISC.brandMark` + `MISC.brandSuffix` split) updated. New constants block for the creator detail page copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-banani-delta-polish/07-RESEARCH.md
@.planning/banani/PHASE-7-DELTA.md
@.planning/banani/screens/phase-7/creator-cagnotte-detail.md
@.planning/banani/screens/phase-7/navbar-logo.md
@.planning/phases/06-authed-money-screens/06-02-SUMMARY.md
@CLAUDE.md
@src/components/layout/PublicNavbar.tsx
@src/components/layout/DashboardNavbar.tsx
@src/components/cagnottes/CampaignCard.tsx
@src/lib/constants.ts
@src/app/(authed)/tableau-de-bord/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 0: Safety rails — constants update (logo + creator detail labels)</name>
  <files>src/lib/constants.ts</files>
  <action>
    Open `src/lib/constants.ts` and:

    1. Update the brand identity constants. EITHER:
       - Update `MISC.siteName` from current value (likely `"Cagnottes.sn"` or similar) to `"cagnotte.sn"` (lowercase, singular cagnotte), OR
       - Add a split: `MISC.brandMark = "cagnotte"` + `MISC.brandSuffix = ".sn"` so the navbar JSX can render the two-span pattern without re-splitting the string.

       Pick the split form (cleaner for the navbar JSX). Keep `MISC.siteName = "cagnotte.sn"` for any other consumers (OG tags, footer, etc.).

    2. Add a new `CREATOR_DETAIL_LABELS` exported constant block with the verbatim French labels from `.planning/banani/screens/phase-7/creator-cagnotte-detail.md`. Required keys (use exactly these French strings):
       - `statusOnline: "En ligne"`
       - `subtypeFestive: "Cagnotte Festive"`
       - `subtypeSolidaire: "Cagnotte Solidaire"`
       - `kpiCollected: "Montant récolté"`
       - `kpiParticipants: "Participations"`
       - `kpiAvailableFunds: "Fonds disponibles"`
       - `withdrawCta: "Retirer les fonds"`
       - `withdrawHelper: "Transférez ce montant sur votre compte Mobile Money en quelques clics."`
       - `recentParticipations: "Participations récentes"`
       - `viewAllParticipations: "Voir toutes"`
       - `shareLinkTitle: "Lien de la cagnotte"`
       - `visibilityTitle: "Visibilité"`
       - `dangerZoneTitle: "Zone de danger"`
       - `closeCagnotte: "Clôturer la cagnotte"`
       - `manageCta: "Gérer"`
       - `shareCta: "Partager"`

    3. Do NOT touch any other constants block in this commit.

    No other file is modified in this task. This is the dedicated "safety rails" commit so future tasks can grep & import without ambiguity.
  </action>
  <verify>
    <automated>npm run lint -- src/lib/constants.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    `MISC.brandMark === "cagnotte"`, `MISC.brandSuffix === ".sn"`, `MISC.siteName === "cagnotte.sn"`, and `CREATOR_DETAIL_LABELS` exported with the 16 keys above. ESLint passes. One conventional commit: `chore(07-01): constants for cagnotte.sn brand + creator detail labels`.
  </done>
</task>

<task type="auto">
  <name>Task 1: Logo swap on both navbars (verbatim Banani two-span pattern)</name>
  <files>src/components/layout/PublicNavbar.tsx, src/components/layout/DashboardNavbar.tsx</files>
  <action>
    Reference: `.planning/banani/screens/phase-7/navbar-logo.md` for the exact JSX.

    In BOTH `PublicNavbar.tsx` and `DashboardNavbar.tsx`:

    1. Locate the existing brand link / Logo element (anchor or `<Link>` to `/`).
    2. Replace its content with the verbatim two-span pattern:
       ```tsx
       import { MISC } from "@/lib/constants";
       // ...
       <Link
         href="/"
         className="text-2xl font-black tracking-tighter text-primary flex items-center"
         aria-label={MISC.siteName}
       >
         {MISC.brandMark}
         <span className="text-gray-400 font-medium ml-1 text-lg">{MISC.brandSuffix}</span>
       </Link>
       ```
    3. Remove any prior logo image / SVG / Image component if it was used. Pure text logo (no image asset).
    4. Keep all surrounding navbar structure (mobile menu, links, auth buttons) unchanged.
    5. The `flex items-center` on the anchor itself ensures the `.sn` span baselines correctly with the larger `cagnotte` text.
    6. Use `text-primary` (Tailwind v4 token mapped to navy `#172866`).

    Do NOT touch any other navbar logic — only the brand link.
  </action>
  <verify>
    <automated>npm run lint -- src/components/layout/PublicNavbar.tsx src/components/layout/DashboardNavbar.tsx 2>&1 | tail -20 && grep -n "brandMark" src/components/layout/PublicNavbar.tsx src/components/layout/DashboardNavbar.tsx</automated>
  </verify>
  <done>
    Both files import `MISC` from `@/lib/constants`, both render the two-span pattern with `text-2xl font-black tracking-tighter text-primary flex items-center` on the anchor and `text-gray-400 font-medium ml-1 text-lg` on the suffix span. ESLint passes. One commit: `feat(07-01): two-tone lowercase cagnotte.sn logo on both navbars`.
  </done>
</task>

<task type="auto">
  <name>Task 2: CampaignCard linkVariant prop + dashboard call-site update</name>
  <files>src/components/cagnottes/CampaignCard.tsx, src/app/(authed)/tableau-de-bord/page.tsx</files>
  <action>
    1. **CampaignCard.tsx**:
       - Add a new optional prop to the component's TypeScript interface:
         ```ts
         linkVariant?: "public" | "creator";
         ```
         with a default of `"public"`.
       - Locate the link/anchor element that wraps the card body. It currently routes to `/c/${slug}` (or similar `block.slug`).
       - Compute the href:
         ```ts
         const href = linkVariant === "creator"
           ? `/tableau-de-bord/cagnottes/${slug}`
           : `/c/${slug}`;
         ```
       - Use `href` on the `<Link>` (Next.js `next/link`).
       - DO NOT touch any other prop, styling, or sub-component — purely additive.

    2. **Dashboard page (src/app/(authed)/tableau-de-bord/page.tsx)**:
       - Find every `<CampaignCard ... />` usage on the dashboard (likely inside a "recent cagnottes" / "mes cagnottes" list — may also live inside a client island like `DashboardCagnotteList.tsx`; if so, also update that file and add it to `files_modified` in the commit).
       - Add `linkVariant="creator"` to each dashboard usage.
       - Verify (grep) that public discovery pages (`src/app/(public)/page.tsx`, `src/app/(public)/toutes-les-cagnottes/page.tsx`, etc.) do NOT pass `linkVariant` — they must rely on the `"public"` default.

    3. Run a sanity grep:
       ```
       grep -rn "<CampaignCard" src/
       ```
       Confirm only the dashboard usage(s) carry `linkVariant="creator"`.
  </action>
  <verify>
    <automated>npm run lint -- src/components/cagnottes/CampaignCard.tsx src/app/\(authed\)/tableau-de-bord/page.tsx 2>&1 | tail -20 && grep -rn "linkVariant" src/components/cagnottes src/app</automated>
  </verify>
  <done>
    `CampaignCard` accepts `linkVariant?: "public" | "creator"`, defaults to `"public"`. Dashboard call-site(s) pass `linkVariant="creator"`. Public call-sites unchanged. ESLint green. One commit: `feat(07-01): CampaignCard linkVariant prop + dashboard creator routing`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Creator cagnotte detail page (server component, owner-gated)</name>
  <files>src/app/(authed)/tableau-de-bord/cagnottes/[slug]/page.tsx</files>
  <action>
    Reference layout: `.planning/banani/screens/phase-7/creator-cagnotte-detail.md` (mirrors `DashboardCagnotteDetail.jsx` from the Banani export).

    Create `src/app/(authed)/tableau-de-bord/cagnottes/[slug]/page.tsx` as a **Next.js 16 App Router server component** (no `"use client"`).

    1. **Imports**:
       - `import { notFound } from "next/navigation";`
       - `import Link from "next/link";`
       - `import { cookies } from "next/headers";`
       - `import { CREATOR_DETAIL_LABELS, MISC } from "@/lib/constants";`
       - `import { formatPrice } from "@/lib/utils";` (use existing helper)
       - Any existing server-side fetcher helper from `@/lib/server-api` if one exists; otherwise fetch directly with `process.env.NEXT_PUBLIC_API_URL` + the `izy-token` cookie forwarded as a header.

    2. **Owner gate** (server-side):
       - Read the `izy-token` cookie via `cookies()`.
       - Decode the JWT payload to get `sub` (seller id) — reuse any existing helper. If no helper exists, decode the JWT payload manually with `Buffer.from(parts[1], 'base64url').toString()` and `JSON.parse` (NO signature verification needed here — the backend re-checks). Comment: `// signature verification deferred to backend; this is a UX gate only`.
       - Fetch `GET ${API_URL}/api/cagnottes/${slug}` forwarding the cookie. If 404 → `notFound()`. If response.block.sellerId !== payload.sub → `notFound()` (do NOT 403 — pretend the cagnotte does not exist for non-owners).

    3. **Data fetches** (parallelized with `Promise.all`):
       - `GET /api/cagnottes/:slug` → `{ block, progress }` (already fetched in owner gate; reuse).
       - `GET /api/cagnottes/:slug/participants?limit=5` → recent list.
       - `GET /api/withdrawals/balance` → `{ availableAmount, pendingAmount }`. If 403 (KYC not approved) catch and treat as `availableAmount = 0` with a note.

    4. **Render** (top-down per the wireframe):

       a. **Header row** (mobile-first stack, `md:flex-row` desktop):
          - `<img src={block.config.coverUrl} className="w-20 h-20 rounded-2xl object-cover"/>` (use `/api/files/:key` proxy — backend already rewrites)
          - Stack: `<Badge>{CREATOR_DETAIL_LABELS.statusOnline}</Badge>` (green pill), then `<h1 className="font-poppins font-black text-3xl text-primary">{block.config.title}</h1>`, then sub-label `{block.config.subtype === "festive" ? CREATOR_DETAIL_LABELS.subtypeFestive : CREATOR_DETAIL_LABELS.subtypeSolidaire}`
          - Right side: `<Link href={\`/tableau-de-bord/cagnottes/${slug}/modifier\`}>{CREATOR_DETAIL_LABELS.manageCta}</Link>` (outline button) + `<button>{CREATOR_DETAIL_LABELS.shareCta}</button>` (primary button — wired to client-side share sheet via a small client island if needed; for now a plain button is acceptable).

       b. **2-col KPI grid** (`grid grid-cols-1 md:grid-cols-2 gap-4`):
          - Card 1: label `{CREATOR_DETAIL_LABELS.kpiCollected}`, value `{formatPrice(progress.collected)}`, sub `{formatPrice(block.config.goalAmount)}` goal.
          - Card 2: label `{CREATOR_DETAIL_LABELS.kpiParticipants}`, value `{progress.donorCount}`.

       c. **Withdraw Action Box** (full-width, `border-2 border-primary rounded-2xl p-6 relative overflow-hidden`):
          - Decorative pink blob: `<div className="absolute -right-8 -top-8 w-32 h-32 rounded-full bg-accent opacity-50"/>` (z-0 behind content).
          - Label `{CREATOR_DETAIL_LABELS.kpiAvailableFunds}` + big amount `{formatPrice(balance.availableAmount)}`.
          - Helper `<p>{CREATOR_DETAIL_LABELS.withdrawHelper}</p>`.
          - Primary CTA `<Link href={\`/retraits?blockId=${block.id}\`} className="...">{CREATOR_DETAIL_LABELS.withdrawCta}</Link>`.

       d. **Main column / sidebar split** (`grid grid-cols-1 lg:grid-cols-3 gap-6`):
          - **Main (col-span-2)** — Recent participations:
            - Header row: `<h2>{CREATOR_DETAIL_LABELS.recentParticipations}</h2>` + `<Link href={\`/tableau-de-bord/cagnottes/${slug}/stats\`}>{CREATOR_DETAIL_LABELS.viewAllParticipations}</Link>`
            - List: each participant rendered as a row with an initial-circle avatar (`w-10 h-10 rounded-full bg-accent text-primary font-bold flex items-center justify-center`), name (use real `donorDisplayName` from server API — backend never masks for owner endpoint), `formatPrice(amount)`, optional italic message in `<p className="italic text-gray-500 text-sm mt-1">"{message}"</p>`.
            - Empty state: `EmptyState` primitive with copy "Aucune participation pour l'instant." (add to constants if it does not already exist).
          - **Sidebar (col-span-1)**:
            - **Card: Lien de la cagnotte** — header + readonly input showing `${MISC.siteName}/c/${slug}` + Copy/WhatsApp buttons (small client island acceptable: `CopyShareIsland.tsx` if not already shipped). For v1 minimal: render the URL in a styled `<input readOnly>` and a `<button>` that triggers a client-side `navigator.clipboard.writeText` (this requires a tiny client component; add `"use client"` at the top of a sub-component file `src/app/(authed)/tableau-de-bord/cagnottes/[slug]/_components/CopyLinkBox.tsx`).
            - **Card: Visibilité** — read-only display of current visibility (`block.config.visibility === "public" ? "Publique" : "Privée"`) with a "Modifier" link to `/tableau-de-bord/cagnottes/${slug}/modifier`. Do NOT add toggles here — full toggle UX lives on the modifier page.
            - **Card: Zone de danger** — `border border-red-200 bg-red-50 rounded-2xl p-4` with `<button className="text-red-600">{CREATOR_DETAIL_LABELS.closeCagnotte}</button>`. Wire the click to a confirm dialog that POSTs/PATCHes `endDate = now` (DEFER actual close API: for v1 of this page, the button can route to `/tableau-de-bord/cagnottes/${slug}/modifier#cloturer` — a hash anchor on the modifier page. Add a TODO comment: `// TODO PHASE-8: in-place close action`.)

    5. **No inline French strings** — every label sources from `CREATOR_DETAIL_LABELS`. Use JSX brace expressions for any apostrophes (e.g., `{"l'instant"}`).

    6. **No new dependencies**.

    7. The page MUST be a server component for the SEO/auth gate. Any interactive element (copy button, share sheet) goes into a small client island under `_components/`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -40</automated>
  </verify>
  <done>
    File exists at `src/app/(authed)/tableau-de-bord/cagnottes/[slug]/page.tsx`, is a server component, performs server-side owner check via `izy-token` cookie, fetches the three endpoints, renders header / KPIs / Withdraw Action Box / Recent Participations / Sidebar (3 cards). `npm run build` completes 0 errors 0 new warnings. Manual smoke (Wave 4): visit `/tableau-de-bord` logged in → click a cagnotte → lands on `/tableau-de-bord/cagnottes/{slug}` (NOT `/c/{slug}`). One commit: `feat(07-01): creator cagnotte detail page from DashboardCagnotteDetail`.
  </done>
</task>

<task type="auto">
  <name>Task 4: Final verification + 07-01-SUMMARY.md</name>
  <files>.planning/phases/07-banani-delta-polish/07-01-SUMMARY.md</files>
  <action>
    1. Run the full verification battery:
       ```
       npm run lint
       npm run build
       cd backend && npm run build && cd ..
       bash scripts/verify-ring-purity.sh   # if it exists; skip silently if not
       grep -rn "linkVariant=\"creator\"" src/
       grep -rn "CampaignCard" src/app/\(public\) | grep -v "linkVariant" | head -5  # public usages must NOT carry the prop
       grep -rn "brandMark" src/components/layout/
       grep -rn "Cagnottes.sn" src/  # should return zero matches in JSX (case-sensitive title-case form)
       ```

    2. Create `.planning/phases/07-banani-delta-polish/07-01-SUMMARY.md` following the template at `~/.claude/get-shit-done/templates/summary.md`. It MUST include:
       - **Phase / Plan** header
       - **Requirements closed**: PLSH-01, PLSH-02, PLSH-03 (each with one-line evidence)
       - **Files created** (1) and **Files modified** (5)
       - **Commits** (4 atomic conventional commits, one per task)
       - **Verification results** (lint / build / build-backend / grep guards) all green
       - **Manual QA checklist** (3 items: dashboard click → creator detail; navbar shows lowercase logo on `/` and `/tableau-de-bord`; public discovery card click still goes to `/c/{slug}`)
       - **Risks / follow-ups for 07-02**: the share button on the new detail page is a stub until the share sheet client island lands; the "Clôturer la cagnotte" button currently routes to the modifier page anchor (in-place close action deferred to a future phase).

    3. Commit the SUMMARY: `docs(07-01): SUMMARY for P0 gap fixes`.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-banani-delta-polish/07-01-SUMMARY.md && grep -q "PLSH-01" .planning/phases/07-banani-delta-polish/07-01-SUMMARY.md</automated>
  </verify>
  <done>
    `npm run lint`, `npm run build`, and `cd backend && npm run build` all green. SUMMARY file exists with all required sections. PLSH-01, PLSH-02, PLSH-03 marked closed in the SUMMARY. One commit: `docs(07-01): SUMMARY for P0 gap fixes`.
  </done>
</task>

</tasks>

<verification>
- `npm run lint` 0 new warnings in modified files
- `npm run build` 0 errors
- `cd backend && npm run build` 0 errors
- Manual QA: dashboard CampaignCard click → `/tableau-de-bord/cagnottes/[slug]` (NOT `/c/[slug]`)
- Manual QA: both navbars show `cagnotte` + faded `.sn` two-span logo
- Grep: `linkVariant="creator"` appears only on the dashboard call-site
- Grep: no `Cagnottes.sn` (title-case) in JSX — only `cagnotte.sn` lowercase
- Server-side owner check: visiting `/tableau-de-bord/cagnottes/{otherSellerSlug}` returns 404
</verification>

<success_criteria>
- PLSH-01 closed: creator cagnotte detail page renders the DashboardCagnotteDetail layout with all sections.
- PLSH-02 closed: `CampaignCard` accepts `linkVariant`, defaults to `"public"`, dashboard usage passes `"creator"`.
- PLSH-03 closed: both navbars + `MISC.siteName` reflect the lowercase two-tone `cagnotte.sn` brand.
- 4 atomic commits (T0 chore + T1/T2/T3 feat + T4 docs).
- SUMMARY.md exists at `.planning/phases/07-banani-delta-polish/07-01-SUMMARY.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/07-banani-delta-polish/07-01-SUMMARY.md` per template.
</output>
