---
phase: 07-banani-delta-polish
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - backend/src/lib/blocks/schemas.ts
  - src/components/ui/Calendar.tsx
  - src/components/ui/Combobox.tsx
  - src/components/ui/index.ts
  - src/lib/constants.ts
  - src/app/(authed)/tableau-de-bord/nouvelle/festive/etape-1/page.tsx
  - src/app/(authed)/tableau-de-bord/nouvelle/festive/etape-2/page.tsx
  - src/app/(authed)/tableau-de-bord/nouvelle/festive/etape-3/page.tsx
  - src/app/(authed)/tableau-de-bord/nouvelle/solidaire/etape-2/page.tsx
  - src/app/(authed)/tableau-de-bord/nouvelle/solidaire/etape-3/page.tsx
  - src/app/(authed)/tableau-de-bord/cagnottes/[slug]/modifier/page.tsx
  - src/app/(public)/c/[slug]/merci/page.tsx
  - src/components/cagnottes/CampaignCard.tsx
  - src/components/ui/Button.tsx
autonomous: true
requirements: [PLSH-06, PLSH-07, PLSH-08]
must_haves:
  truths:
    - "fundraiserBlockConfigSchema accepts an optional thankYouMessage (max 500 chars, nullable) on both festive and solidaire branches with NO Prisma migration."
    - "Calendar primitive exists at src/components/ui/Calendar.tsx as a Ring 1 popover with custom-drawn month grid + click-outside close + ≥48px touch targets + keyboard nav."
    - "Combobox primitive exists at src/components/ui/Combobox.tsx as a Ring 1 dropdown with custom option rendering + click-outside close + keyboard nav."
    - "Wizard festive/solidaire étape 2 use the new <Calendar> for the end-date field; native <input type=\"date\"> is removed from those routes."
    - "Wizard festive étape 1 uses the new <Combobox> for the Occasion field; native <select> is removed from that route."
    - "Wizard festive + solidaire étape 3 capture an optional thankYouMessage (Textarea + 0/500 counter) which round-trips into Block.config via POST /api/blocks."
    - "/c/[slug]/merci renders block.config.thankYouMessage (when present) below the hero, alongside order.reference styled as a font-mono confirmation code."
    - "/tableau-de-bord/cagnottes/[slug]/modifier exposes the thankYouMessage field for editing."
    - "Zero new npm dependencies (git diff package.json empty)."
  artifacts:
    - path: "backend/src/lib/blocks/schemas.ts"
      provides: "fundraiserBlockConfigSchema extended with thankYouMessage"
      contains: "thankYouMessage"
    - path: "src/components/ui/Calendar.tsx"
      provides: "Ring 1 popover Calendar primitive"
      min_lines: 80
    - path: "src/components/ui/Combobox.tsx"
      provides: "Ring 1 Combobox primitive"
      min_lines: 60
    - path: "src/app/(public)/c/[slug]/merci/page.tsx"
      provides: "Renders thankYouMessage + order.reference confirmation code"
      contains: "thankYouMessage"
  key_links:
    - from: "src/app/(authed)/tableau-de-bord/nouvelle/festive/etape-2/page.tsx"
      to: "src/components/ui/Calendar.tsx"
      via: "import + render Calendar replacing DatePicker / native date input"
      pattern: "Calendar"
    - from: "src/app/(authed)/tableau-de-bord/nouvelle/solidaire/etape-2/page.tsx"
      to: "src/components/ui/Calendar.tsx"
      via: "import + render Calendar"
      pattern: "Calendar"
    - from: "src/app/(authed)/tableau-de-bord/nouvelle/festive/etape-1/page.tsx"
      to: "src/components/ui/Combobox.tsx"
      via: "import + render Combobox replacing native select"
      pattern: "Combobox"
    - from: "src/app/(authed)/tableau-de-bord/nouvelle/festive/etape-3/page.tsx"
      to: "POST /api/blocks"
      via: "thankYouMessage in config payload, Zod schema accepts it"
      pattern: "thankYouMessage"
    - from: "src/app/(public)/c/[slug]/merci/page.tsx"
      to: "block.config.thankYouMessage + order.reference"
      via: "render"
      pattern: "thankYouMessage|reference"
---

<objective>
Phase 7, Plan 03 — P1 tail: 2 new Ring 1 primitives (`Calendar` popover + `Combobox`) + the `thankYouMessage` feature end-to-end (Zod additive + wizard capture + creator edit + donor merci render) + a micro-interactions polish sweep.

Purpose: Close the last three Phase 7 gaps in one cohesive plan. The new primitives unlock Banani-parity wizard step 1 (Occasion combobox) and step 2 (Calendar popover), and the thank-you message feature gives creators a personal touchpoint on the donor merci page. Finally, the polish sweep harmonizes hover transitions and success animations across the whole product.

Output:
- `backend/src/lib/blocks/schemas.ts` extended (additive Zod, no Prisma migration — `Block.config` is already `Json`).
- `src/components/ui/Calendar.tsx` (Ring 1 pure popover with custom month grid).
- `src/components/ui/Combobox.tsx` (Ring 1 pure dropdown with rich option rendering).
- Wizard etape-2 (festive + solidaire) wired to `<Calendar>`.
- Wizard etape-1 festive (and solidaire if it uses a select for cause) wired to `<Combobox>`.
- Wizard etape-3 captures `thankYouMessage` via Textarea + counter; persists into sessionStorage draft + POST.
- `/tableau-de-bord/cagnottes/[slug]/modifier` exposes the field.
- `/c/[slug]/merci` renders the thank-you message + order reference.
- Polish sweep: `CampaignCard` hover, `Button` transition-colors, success-page `animate-ping`, wizard pill transitions.
- Zero new deps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-banani-delta-polish/07-RESEARCH.md
@.planning/banani/PHASE-7-DELTA.md
@.planning/banani/screens/phase-7/festive-occasion-dropdown-open.md
@.planning/banani/screens/phase-7/solidaire-step-2-date-field.md
@.planning/banani/screens/phase-7/participation-success.md
@.planning/phases/07-banani-delta-polish/07-01-SUMMARY.md
@.planning/phases/07-banani-delta-polish/07-02-SUMMARY.md
@CLAUDE.md
@backend/src/lib/blocks/schemas.ts
@src/components/ui/index.ts
@src/components/ui/DatePicker.tsx
@src/components/ui/Select.tsx
@src/components/ui/Button.tsx
@src/components/cagnottes/CampaignCard.tsx
@src/lib/constants.ts
@src/app/(authed)/tableau-de-bord/nouvelle/festive/etape-1/page.tsx
@src/app/(authed)/tableau-de-bord/nouvelle/festive/etape-2/page.tsx
@src/app/(authed)/tableau-de-bord/nouvelle/festive/etape-3/page.tsx
@src/app/(authed)/tableau-de-bord/nouvelle/solidaire/etape-2/page.tsx
@src/app/(authed)/tableau-de-bord/nouvelle/solidaire/etape-3/page.tsx
@src/app/(authed)/tableau-de-bord/cagnottes/[slug]/modifier/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Extend fundraiserBlockConfigSchema with thankYouMessage (BACKEND)</name>
  <files>backend/src/lib/blocks/schemas.ts</files>
  <behavior>
    - thankYouMessage may be undefined → schema passes (optional).
    - thankYouMessage may be null → schema passes (nullable).
    - thankYouMessage = "" empty string → schema passes (treated as no message).
    - thankYouMessage = "x".repeat(500) → schema passes (boundary).
    - thankYouMessage = "x".repeat(501) → schema rejects with max(500) error.
    - thankYouMessage = 42 (non-string) → schema rejects.
    - The field is accepted on BOTH `subtype: "festive"` and `subtype: "solidaire"` branches.
  </behavior>
  <action>
    1. Open `backend/src/lib/blocks/schemas.ts`. Locate `fundraiserBlockConfigSchema` (the Zod schema with the `superRefine` for festive/solidaire validation).

    2. Add `thankYouMessage` to the **base shape** (not inside the superRefine) so it applies to both subtypes:
       ```ts
       thankYouMessage: z.string().max(500).nullable().optional(),
       ```
       Place it next to the existing optional fields (e.g., near `hideAmount`, `hideDonors`, or wherever the other optional config fields live).

    3. **CRITICAL — verify NO Prisma migration is needed**: open `backend/prisma/schema.prisma` and confirm `Block.config` is `Json` type. If it is, no migration. If it is somehow `Json?` or differently typed, STOP and consult — but per CLAUDE.md and prior context, it is `Json`.

    4. Run `cd backend && npm run build` — must complete with 0 errors.

    5. Add a quick test (or reuse the existing schema test harness if `backend/scripts/test-schemas.ts` exists). If no test harness exists, create a minimal `backend/scripts/test-thankyou-schema.ts` (one-off, ~30 lines) that exercises the 7 behaviors from `<behavior>` above using `fundraiserBlockConfigSchema.safeParse(...)`. Run it with `npx tsx`. If it is too costly to add, skip the test file and rely on the build + runtime smoke (acceptable for an additive optional field).

    6. Atomic commit: `feat(07-03): add thankYouMessage to fundraiserBlockConfigSchema (additive)`.
  </action>
  <verify>
    <automated>cd backend && npm run build 2>&1 | tail -10 && grep -n "thankYouMessage" backend/src/lib/blocks/schemas.ts</automated>
  </verify>
  <done>
    `fundraiserBlockConfigSchema` accepts `thankYouMessage: z.string().max(500).nullable().optional()` on both subtypes. `cd backend && npm run build` exits 0. Behavior tests pass (or build alone is the gate if no harness was added). Confirmed `Block.config` is already `Json` — NO Prisma migration. One commit.
  </done>
</task>

<task type="auto">
  <name>Task 1: <Calendar> popover primitive (Ring 1 pure)</name>
  <files>src/components/ui/Calendar.tsx, src/components/ui/index.ts</files>
  <action>
    Create `src/components/ui/Calendar.tsx` as a **Ring 1 pure** popover Calendar component.

    **Ring 1 purity rules** (CRITICAL):
    - NO imports from `@/lib/api`, `@/lib/useApi`, `@/contexts/*`, `@/lib/constants`.
    - Only React + `lucide-react` (already a dependency) + `clsx`/`tailwind-merge` if needed via the existing `cn` helper from `@/lib/utils` (verify `@/lib/utils` is allowed by the ring purity script — typically it is).

    **Props interface**:
    ```ts
    interface CalendarProps {
      value: Date | null;
      onChange: (date: Date | null) => void;
      minDate?: Date;
      maxDate?: Date;
      placeholder?: string;
      label?: string;
      error?: string;
      helper?: string;
      clearable?: boolean;
      name?: string;
    }
    ```

    **Structure**:

    1. **Trigger button** (same shell as the restyled `DatePicker` from 07-02 T6):
       ```tsx
       <button
         type="button"
         onClick={() => setOpen(o => !o)}
         className="border border-gray-300 rounded-xl px-4 py-3.5 bg-white text-gray-400 flex justify-between items-center cursor-pointer hover:border-primary transition-colors shadow-sm w-full"
         aria-haspopup="dialog"
         aria-expanded={open}
       >
         <span className={value ? "text-primary font-medium" : ""}>
           {value ? formatFrenchDate(value) : (placeholder ?? "Sélectionnez une date...")}
         </span>
         <CalendarIcon className="w-5 h-5 text-gray-400"/>
       </button>
       ```

    2. **Popover** — absolutely positioned below the trigger:
       ```tsx
       {open && (
         <div
           ref={popoverRef}
           role="dialog"
           aria-label="Sélecteur de date"
           className="absolute z-50 mt-2 bg-white border border-gray-200 rounded-2xl shadow-xl p-4 w-[320px] max-w-[calc(100vw-2rem)]"
         >
           {/* header: month/year + prev/next */}
           {/* weekday row */}
           {/* day grid */}
         </div>
       )}
       ```

    3. **Header**:
       ```tsx
       <div className="flex items-center justify-between mb-4">
         <button type="button" onClick={prevMonth} className="w-10 h-10 rounded-lg hover:bg-gray-100 flex items-center justify-center" aria-label="Mois précédent">
           <ChevronLeft className="w-5 h-5"/>
         </button>
         <span className="font-poppins font-bold text-primary">{monthYearLabel}</span>
         <button type="button" onClick={nextMonth} className="w-10 h-10 rounded-lg hover:bg-gray-100 flex items-center justify-center" aria-label="Mois suivant">
           <ChevronRight className="w-5 h-5"/>
         </button>
       </div>
       ```

    4. **Weekday row** (French abbreviations starting Monday — Senegalese week convention):
       ```tsx
       const WEEKDAYS = ["L", "M", "M", "J", "V", "S", "D"];
       <div className="grid grid-cols-7 gap-1 mb-2">
         {WEEKDAYS.map((d, i) => (
           <div key={i} className="text-center text-xs text-gray-400 font-bold py-2">{d}</div>
         ))}
       </div>
       ```

    5. **Day grid** (6 rows × 7 cols, computed from `viewMonth`):
       ```tsx
       <div className="grid grid-cols-7 gap-1">
         {days.map((day, i) => {
           if (!day) return <div key={i}/>; // empty cell for leading/trailing
           const isSelected = value && sameDay(day, value);
           const isToday = sameDay(day, new Date());
           const isDisabled = (minDate && day < minDate) || (maxDate && day > maxDate);
           return (
             <button
               key={i}
               type="button"
               disabled={isDisabled}
               onClick={() => { onChange(day); setOpen(false); }}
               className={cn(
                 "w-10 h-10 rounded-full flex items-center justify-center text-sm transition-colors",
                 isSelected && "bg-primary text-white font-bold",
                 !isSelected && isToday && "border-2 border-primary",
                 !isSelected && !isToday && "hover:bg-gray-100 text-primary",
                 isDisabled && "text-gray-300 cursor-not-allowed hover:bg-transparent"
               )}
               aria-label={day.toLocaleDateString("fr-FR")}
               aria-selected={isSelected}
             >
               {day.getDate()}
             </button>
           );
         })}
       </div>
       ```

    6. **Month grid generation** — pure native `Date` arithmetic (NO date library):
       ```ts
       function buildMonthGrid(year: number, month: number): (Date | null)[] {
         const firstOfMonth = new Date(year, month, 1);
         const lastOfMonth = new Date(year, month + 1, 0);
         const daysInMonth = lastOfMonth.getDate();
         // Monday-first index: (getDay() + 6) % 7 — Sunday=0 becomes 6
         const startOffset = (firstOfMonth.getDay() + 6) % 7;
         const cells: (Date | null)[] = [];
         for (let i = 0; i < startOffset; i++) cells.push(null);
         for (let d = 1; d <= daysInMonth; d++) cells.push(new Date(year, month, d));
         while (cells.length < 42) cells.push(null); // 6 rows × 7 cols
         return cells;
       }
       ```

    7. **French month label**:
       ```ts
       const FRENCH_MONTHS = ["janvier","février","mars","avril","mai","juin","juillet","août","septembre","octobre","novembre","décembre"];
       const monthYearLabel = `${FRENCH_MONTHS[viewMonth]} ${viewYear}`;
       ```

    8. **Click-outside close** (useEffect + ref):
       ```tsx
       useEffect(() => {
         if (!open) return;
         const handler = (e: MouseEvent) => {
           if (popoverRef.current && !popoverRef.current.contains(e.target as Node) && !triggerRef.current?.contains(e.target as Node)) {
             setOpen(false);
           }
         };
         document.addEventListener("mousedown", handler);
         return () => document.removeEventListener("mousedown", handler);
       }, [open]);
       ```

    9. **Keyboard navigation** (minimum):
       - `Escape` closes the popover.
       - Arrow keys navigate between days (focus management on the day buttons).
       - `Enter` selects the focused day.
       Acceptable v1: handle Escape + Tab cycling. Full arrow-key nav can be a TODO if it gets too long.

    10. **Mobile-first**:
        - Touch targets ≥ 48px: day buttons are `w-10 h-10` (40px) — bump to `w-12 h-12` on mobile via `md:w-10 md:h-10` if necessary, OR rely on the parent padding to hit the 48px target. Acceptable: `w-10 h-10` with `gap-1` is ~44px center-to-center which is borderline; use `w-11 h-11` if it fits the 320px popover width.
        - On mobile (`<md:`), the popover should still fit inside the viewport — `max-w-[calc(100vw-2rem)]` ensures this.

    11. **Pure computation** — no date library. Native `Date` only.

    12. Add `Calendar` to the `src/components/ui/index.ts` barrel export.

    13. Run the ring purity script:
        ```
        bash scripts/verify-ring-purity.sh
        ```
        (Skip silently if the script does not exist.)

    14. Atomic commit: `feat(07-03): Calendar popover primitive (Ring 1)`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10 && grep -E "from \"@/lib/(api|useApi|constants)\"|from \"@/contexts" src/components/ui/Calendar.tsx | head -5 && grep -n "Calendar" src/components/ui/index.ts</automated>
  </verify>
  <done>
    `Calendar.tsx` exists, Ring 1 pure, exports a popover Calendar with custom month grid, click-outside close, French weekday/month labels, ≥40px touch targets, mobile viewport-safe. Barrel exported. Build green. Ring purity script (if present) green. One commit.
  </done>
</task>

<task type="auto">
  <name>Task 2: <Combobox> primitive (Ring 1 pure)</name>
  <files>src/components/ui/Combobox.tsx, src/components/ui/index.ts</files>
  <action>
    Reference: `.planning/banani/screens/phase-7/festive-occasion-dropdown-open.md` (mirrors `CreateFestiveCagnotteStep1OccasionOpen.jsx`).

    Create `src/components/ui/Combobox.tsx` as a **Ring 1 pure** dropdown combobox.

    **Props interface**:
    ```ts
    interface ComboboxOption {
      value: string;
      label: string;
      icon?: React.ReactNode; // emoji or lucide icon
      separatorBefore?: boolean;
    }

    interface ComboboxProps {
      value: string;
      onChange: (value: string) => void;
      options: ComboboxOption[];
      placeholder?: string;
      label?: string;
      error?: string;
      name?: string;
    }
    ```

    **Structure**:

    1. **Trigger button**:
       ```tsx
       <button
         type="button"
         ref={triggerRef}
         onClick={() => setOpen(o => !o)}
         className="border border-gray-300 rounded-xl px-4 py-3.5 bg-white text-gray-400 flex justify-between items-center cursor-pointer hover:border-primary focus:border-primary focus:outline-none transition-colors shadow-sm w-full"
         aria-haspopup="listbox"
         aria-expanded={open}
       >
         <span className={selected ? "text-primary font-medium flex items-center gap-2" : ""}>
           {selected?.icon}
           {selected?.label ?? (placeholder ?? "Sélectionnez une option...")}
         </span>
         <ChevronDown className={cn("w-5 h-5 text-gray-400 transition-transform", open && "rotate-180")}/>
       </button>
       ```

    2. **Popover**:
       ```tsx
       {open && (
         <div
           ref={popoverRef}
           role="listbox"
           className="absolute z-50 mt-2 bg-white border border-gray-200 rounded-2xl shadow-xl overflow-hidden py-2 w-full"
         >
           {options.map((opt, i) => (
             <Fragment key={opt.value}>
               {opt.separatorBefore && <div className="h-px bg-gray-100 my-1 mx-4"/>}
               <button
                 type="button"
                 role="option"
                 aria-selected={value === opt.value}
                 onClick={() => { onChange(opt.value); setOpen(false); }}
                 className="px-5 py-3.5 hover:bg-gray-50 text-primary font-bold flex items-center justify-between w-full text-left transition-colors group"
               >
                 <span className="flex items-center gap-3">
                   {opt.icon}
                   {opt.label}
                 </span>
                 <Check className={cn("w-5 h-5 transition-opacity", value === opt.value ? "opacity-100" : "opacity-0 group-hover:opacity-50")}/>
               </button>
             </Fragment>
           ))}
         </div>
       )}
       ```

    3. **Click-outside close** — same useEffect pattern as Calendar.

    4. **Keyboard navigation**:
       - `ArrowDown` / `ArrowUp` move focus through option buttons.
       - `Enter` selects the focused option.
       - `Escape` closes.
       Acceptable v1: handle Escape + native Tab order. Arrow nav is a stretch goal; if it adds > 30 lines, defer with a TODO.

    5. **Ring 1 purity** — same rules as Calendar. NO `@/lib/api`, NO `@/lib/useApi`, NO `@/contexts`, NO `@/lib/constants`. Strings are passed in via `options` props by the parent.

    6. Add `Combobox` to `src/components/ui/index.ts` barrel.

    7. Run ring purity script.

    8. Atomic commit: `feat(07-03): Combobox primitive (Ring 1)`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10 && grep -E "from \"@/lib/(api|useApi|constants)\"|from \"@/contexts" src/components/ui/Combobox.tsx | head -5 && grep -n "Combobox" src/components/ui/index.ts</automated>
  </verify>
  <done>
    `Combobox.tsx` exists, Ring 1 pure, custom option rendering with icon + check + hover, click-outside close. Barrel exported. Build + ring purity green. One commit.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire <Calendar> into wizard etape-2 (festive + solidaire)</name>
  <files>src/app/(authed)/tableau-de-bord/nouvelle/festive/etape-2/page.tsx, src/app/(authed)/tableau-de-bord/nouvelle/solidaire/etape-2/page.tsx</files>
  <action>
    1. Open `src/app/(authed)/tableau-de-bord/nouvelle/festive/etape-2/page.tsx`.

    2. Locate the "Date de fin" field. Currently it is either:
       - The restyled `<DatePicker>` from 07-02, OR
       - A native `<input type="date">`.

    3. Replace it with the new `<Calendar>` primitive:
       ```tsx
       import { Calendar } from "@/components/ui";
       // ...
       <Calendar
         value={endDate ? new Date(endDate) : null}
         onChange={(date) => setEndDate(date ? date.toISOString().slice(0, 10) : null)}
         minDate={new Date()} // no past dates
         placeholder="Sélectionnez la date de fin..."
         label="Date de fin"
       />
       ```

    4. Verify the existing sessionStorage draft key shape is unchanged. The wizard already serializes `endDate` as an ISO date string (`YYYY-MM-DD`); the Calendar component returns a `Date` object that the parent converts via `toISOString().slice(0, 10)`. NO change to the draft schema.

    5. Repeat the same change in `src/app/(authed)/tableau-de-bord/nouvelle/solidaire/etape-2/page.tsx`.

    6. Verify (grep) that no `<input type="date">` remains in either file:
       ```
       grep -n "input.*type=\"date\"" src/app/\(authed\)/tableau-de-bord/nouvelle/festive/etape-2/page.tsx src/app/\(authed\)/tableau-de-bord/nouvelle/solidaire/etape-2/page.tsx
       ```
       Should return nothing.

    7. Mobile-first 375px viewport check: the popover fits inside the viewport with `max-w-[calc(100vw-2rem)]` from the Calendar primitive.

    8. Atomic commit: `feat(07-03): wire Calendar into wizard etape-2 (festive+solidaire)`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10 && ! grep -n "input.*type=\"date\"" src/app/\(authed\)/tableau-de-bord/nouvelle/festive/etape-2/page.tsx src/app/\(authed\)/tableau-de-bord/nouvelle/solidaire/etape-2/page.tsx</automated>
  </verify>
  <done>
    Both etape-2 pages import + render `<Calendar>` for the end-date field. Native `<input type="date">` removed. Draft sessionStorage shape unchanged. Build green. One commit.
  </done>
</task>

<task type="auto">
  <name>Task 4: Wire <Combobox> into wizard festive étape 1 (Occasion)</name>
  <files>src/app/(authed)/tableau-de-bord/nouvelle/festive/etape-1/page.tsx, src/lib/constants.ts</files>
  <action>
    1. Open `src/lib/constants.ts` and verify (or add) the festive occasion options:
       ```ts
       export const FESTIVE_OCCASIONS = [
         { value: "anniversaire", label: "Anniversaire", icon: "🎂" },
         { value: "mariage", label: "Mariage", icon: "💍" },
         { value: "pot-de-depart", label: "Pot de départ", icon: "👋" },
         { value: "cadeau-commun", label: "Cadeau commun", icon: "🎁" },
         { value: "naissance", label: "Naissance", icon: "👶" },
         { value: "voyage", label: "Voyage", icon: "✈️" },
         { value: "autre", label: "Autre occasion", icon: "✨", separatorBefore: true },
       ];
       ```
       The `separatorBefore: true` on "autre" gives the Banani-style horizontal divider above it.

       Note: emoji is a string, not a ReactNode. The Combobox `icon` prop accepts ReactNode, so the consumer maps `{ icon: <span>{opt.icon}</span> }` when passing options. Alternatively, the Combobox can accept `icon: string | React.ReactNode` and render strings directly. Pick whichever is cleaner.

    2. Open `src/app/(authed)/tableau-de-bord/nouvelle/festive/etape-1/page.tsx`. Locate the "Occasion" field — currently a `<Select>` primitive or a native `<select>`.

    3. Replace it with `<Combobox>`:
       ```tsx
       import { Combobox } from "@/components/ui";
       import { FESTIVE_OCCASIONS } from "@/lib/constants";
       // ...
       <Combobox
         value={occasion}
         onChange={setOccasion}
         options={FESTIVE_OCCASIONS.map(opt => ({
           ...opt,
           icon: <span className="text-2xl">{opt.icon}</span>,
         }))}
         placeholder="Choisissez une occasion..."
         label="Occasion"
       />
       ```

    4. **Solidaire étape 1**: open `src/app/(authed)/tableau-de-bord/nouvelle/solidaire/etape-1/page.tsx` and check whether it uses `<Select>` for the "Cause" field. If yes, also replace it with `<Combobox>` using a `SOLIDAIRE_CAUSES` constant (define in `constants.ts` if it does not already exist). Causes per FUND-02: santé, éducation, projet solidaire, urgence, animaux. If the solidaire page does not use a select, skip.

    5. Verify (grep) that no native `<select>` remains in festive etape-1:
       ```
       grep -n "<select" src/app/\(authed\)/tableau-de-bord/nouvelle/festive/etape-1/page.tsx
       ```
       Should return nothing.

    6. Atomic commit: `feat(07-03): wire Combobox into wizard étape 1 occasion/cause`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10 && ! grep -n "<select" src/app/\(authed\)/tableau-de-bord/nouvelle/festive/etape-1/page.tsx</automated>
  </verify>
  <done>
    Festive etape-1 imports `<Combobox>` and `FESTIVE_OCCASIONS`. Native `<select>` removed. Solidaire etape-1 also migrated if applicable. Build green. One commit.
  </done>
</task>

<task type="auto">
  <name>Task 5: thankYouMessage capture + edit + render end-to-end</name>
  <files>src/app/(authed)/tableau-de-bord/nouvelle/festive/etape-3/page.tsx, src/app/(authed)/tableau-de-bord/nouvelle/solidaire/etape-3/page.tsx, src/app/(authed)/tableau-de-bord/cagnottes/[slug]/modifier/page.tsx, src/app/(public)/c/[slug]/merci/page.tsx, src/lib/constants.ts</files>
  <action>
    1. **Constants** (`src/lib/constants.ts`): add a new block for the thank-you copy:
       ```ts
       export const THANK_YOU_LABELS = {
         fieldLabel: "Message de remerciement (optionnel)",
         fieldPlaceholder: "Ex : Merci infiniment pour votre générosité, votre don me touche beaucoup ❤️",
         counter: (n: number) => `${n}/500 caractères`,
       };

       export const MERCI_LABELS = {
         hero: "Merci ! 🎉",
         heroBody: "Votre participation a bien été enregistrée.",
         thankYouMessageFrom: (name: string) => `Un mot de ${name}`,
         confirmationCode: "Code de confirmation",
         viewCagnotteCta: "Voir la cagnotte",
       };
       ```
       (Adjust if some MERCI_LABELS already exist from Phase 4 — in that case, only add the new keys.)

    2. **Wizard festive étape 3**:
       - Add a `<Textarea>` field below the visibility cards:
         ```tsx
         <div>
           <label className="block font-bold text-primary mb-2">{THANK_YOU_LABELS.fieldLabel}</label>
           <textarea
             value={thankYouMessage}
             onChange={e => setThankYouMessage(e.target.value.slice(0, 500))}
             maxLength={500}
             placeholder={THANK_YOU_LABELS.fieldPlaceholder}
             className="w-full border border-gray-300 rounded-xl px-4 py-3.5 min-h-[120px] focus:border-primary focus:outline-none transition-colors"
           />
           <p className="text-sm text-gray-400 text-right mt-1">{THANK_YOU_LABELS.counter(thankYouMessage.length)}</p>
         </div>
         ```
         (Or use the existing `<Textarea>` primitive if it accepts `maxLength` + counter — preferred.)
       - Persist `thankYouMessage` into the wizard sessionStorage draft alongside the other config fields.
       - On submit, include `thankYouMessage` in the `config` object sent to `POST /api/blocks`.

    3. **Wizard solidaire étape 3**: same as festive — identical Textarea + persistence.

    4. **Creator edit page** (`src/app/(authed)/tableau-de-bord/cagnottes/[slug]/modifier/page.tsx`):
       - Add the same `<Textarea>` field to the form.
       - Initialize from `block.config.thankYouMessage`.
       - Include in the PATCH payload to `PUT /api/blocks/:id`.
       - **Slug guard**: confirm the existing `{ slug, ...editableFields }` destructure pattern (or equivalent) excludes `slug` from the PATCH payload (slug rename is v2 — CLAUDE.md). Add `thankYouMessage` to the editable fields.

    5. **Donor merci page** (`src/app/(public)/c/[slug]/merci/page.tsx`):
       - The page already fetches the cagnotte (or has access to its slug). Fetch (or already has) `block.config.thankYouMessage` and the donor's `order.reference`.
       - Below the existing "Merci ! 🎉" hero, render a pink-accent card when `thankYouMessage` is present:
         ```tsx
         {block.config.thankYouMessage && (
           <section className="bg-accent border border-pink-200 rounded-2xl p-6 mt-6 max-w-xl mx-auto">
             <p className="text-xs uppercase tracking-wider text-primary/60 font-bold mb-2">
               {MERCI_LABELS.thankYouMessageFrom(block.config.title /* or organizer.name */)}
             </p>
             <p className="italic text-primary text-lg leading-relaxed">
               &ldquo;{block.config.thankYouMessage}&rdquo;
             </p>
           </section>
         )}
         ```
       - Below that, a confirmation-code card with the donor's `order.reference`:
         ```tsx
         <section className="bg-white border border-gray-200 rounded-2xl p-5 mt-4 max-w-xl mx-auto text-center">
           <p className="text-xs uppercase tracking-wider text-gray-400 font-bold mb-1">
             {MERCI_LABELS.confirmationCode}
           </p>
           <p className="font-mono text-xl text-primary font-bold">{order.reference}</p>
         </section>
         ```
       - Use `&ldquo;` / `&rdquo;` (or JSX brace expression) for the typographic quotes.
       - Apostrophes via JSX brace expressions.

    6. **No new dependencies**. Pure additions.

    7. Atomic commit: `feat(07-03): thankYouMessage end-to-end (capture+edit+render)`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -15 && grep -rn "thankYouMessage" src/app/\(authed\)/tableau-de-bord/nouvelle/festive/etape-3 src/app/\(authed\)/tableau-de-bord/nouvelle/solidaire/etape-3 src/app/\(authed\)/tableau-de-bord/cagnottes src/app/\(public\)/c</automated>
  </verify>
  <done>
    All 4 pages updated: festive etape-3, solidaire etape-3, modifier, and merci. `thankYouMessage` round-trips from wizard → POST → DB → GET → merci page. 500-char client counter shows. Confirmation code renders in `font-mono`. Build green. One commit.
  </done>
</task>

<task type="auto">
  <name>Task 6: Micro-interactions polish sweep</name>
  <files>src/components/cagnottes/CampaignCard.tsx, src/components/ui/Button.tsx</files>
  <action>
    1. **CampaignCard hover**: add `hover:border-primary hover:shadow-md transition-all` to the card root container in `src/components/cagnottes/CampaignCard.tsx`. Verify the existing class list does not already contain `transition-` to avoid conflicts.

    2. **Button transition**: open `src/components/ui/Button.tsx` and verify the `primary` variant uses `transition-colors` on hover (Banani uses flat colour transitions, not `transition-all`). If it currently uses `transition-all`, downgrade to `transition-colors`. If no transition class is present, add `transition-colors duration-150`.

    3. **Donor /merci animate-ping** (already partially done in 07-02 for `/retraits/succes`):
       - Open `src/app/(public)/c/[slug]/merci/page.tsx` (already touched in T5).
       - Wrap the hero check icon (if one exists) in the same `animate-ping` ring pattern as `/retraits/succes`:
         ```tsx
         <div className="relative w-24 h-24 mx-auto">
           <div className="absolute inset-0 rounded-full bg-[#00B67A]/20 animate-ping"/>
           <div className="absolute inset-0 rounded-full bg-[#E6F3EE] flex items-center justify-center">
             <Check className="w-12 h-12 text-[#00B67A]" strokeWidth={3}/>
           </div>
         </div>
         ```
         If no check icon currently exists on `/merci`, add one above the "Merci ! 🎉" heading.

    4. **Wizard progress pills** — if there is a step indicator component (e.g., `WizardStepper.tsx` or inline pills in each etape page), add `transition-colors` to the pill className so colour changes between steps animate smoothly. If the indicator is duplicated across etape files, only touch the shared component if one exists; otherwise skip rather than diverge per-file.

    5. **Dashboard card grid** hover — verify the card grid wrapper (or each `CampaignCard` via the hover added in step 1) gives a subtle hover. If the dashboard already has its own card wrapper with conflicting hover, leave it alone.

    6. **Constraints**:
       - ZERO Framer Motion. Grep guard: `grep -rn "framer-motion" src/ package.json` must return nothing.
       - ZERO new dependencies. `git diff package.json` empty.
       - ONLY Tailwind utilities + `lucide-react`. No custom keyframes added to `globals.css`.

    7. Atomic commit: `feat(07-03): micro-interactions polish sweep`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10 && ! grep -rn "framer-motion" src/ && git diff package.json | wc -l | grep -q "^0$"</automated>
  </verify>
  <done>
    `CampaignCard` hover adds border + shadow transitions. `Button` primary uses `transition-colors`. Donor `/merci` shows `animate-ping` ring behind the check icon. NO framer-motion anywhere. `git diff package.json` is empty. Build green. One commit.
  </done>
</task>

<task type="auto">
  <name>Task 7: Final verification + 07-03-SUMMARY.md (Phase 7 complete)</name>
  <files>.planning/phases/07-banani-delta-polish/07-03-SUMMARY.md</files>
  <action>
    1. Run the full verification battery:
       ```
       npm run lint
       npm run build
       cd backend && npm run build && cd ..
       bash scripts/verify-ring-purity.sh   # Calendar + Combobox MUST pass
       git diff package.json                # MUST be empty
       grep -rn "framer-motion" src/ package.json  # MUST return nothing
       grep -rn "<select" src/app/\(authed\)/tableau-de-bord/nouvelle/festive/etape-1   # MUST return nothing
       grep -rn "input.*type=\"date\"" src/app/\(authed\)/tableau-de-bord/nouvelle/festive/etape-2 src/app/\(authed\)/tableau-de-bord/nouvelle/solidaire/etape-2  # MUST return nothing
       grep -n "thankYouMessage" backend/src/lib/blocks/schemas.ts
       grep -n "Calendar\|Combobox" src/components/ui/index.ts
       ```

    2. Manual QA checklist (run dev server in two terminals per CLAUDE.md):
       - Festive wizard étape 1 → "Occasion" field opens a custom popover (NOT a native dropdown), shows emoji + label rows, "Autre occasion" sits below a divider, hover shows a check icon, click selects + closes.
       - Festive wizard étape 2 → "Date de fin" field opens a custom popover with month grid, French weekday/month labels, today border, selected pill, click-outside closes, past dates disabled.
       - Festive wizard étape 3 → Textarea for thank-you message visible, counter updates, persists into draft.
       - Submit a new festive cagnotte → backend accepts `thankYouMessage` in config (no Zod error).
       - Visit `/c/{slug}/merci` after a fake donation → see thank-you message card (pink accent) + confirmation code (font-mono).
       - Edit an existing cagnotte at `/tableau-de-bord/cagnottes/{slug}/modifier` → field shows current message, can be updated.
       - Solidaire wizard étape 2 → same Calendar popover.
       - Hover a `CampaignCard` on the dashboard → border + shadow transition visible.

    3. Create `.planning/phases/07-banani-delta-polish/07-03-SUMMARY.md` per template. Include:
       - **Requirements closed**: PLSH-06, PLSH-07, PLSH-08 (each with one-line evidence)
       - **Files created** (3: `Calendar.tsx`, `Combobox.tsx`, SUMMARY) and **Files modified** (~12)
       - **Commits** (8 atomic conventional commits, one per task — T0/T1/T2/T3/T4/T5/T6 feat + T7 docs)
       - **Verification results** (lint / build / backend build / package.json diff empty / framer-motion grep empty / native select+date grep empty / ring purity green)
       - **Phase 7 complete**: append a final paragraph confirming PLSH-01 through PLSH-08 are all closed across plans 07-01/02/03. Include the full requirement coverage check.
       - **Risks / follow-ups for future phases**: in-place "Clôturer la cagnotte" action still routes to a hash anchor (deferred); `<Calendar>` arrow-key nav may be a stretch (note if implemented or deferred); bank account add flow on `/profil/coordonnees-bancaires` is still a stub.

    4. Commit the SUMMARY: `docs(07-03): SUMMARY for new primitives + thank-you feature + polish`.

    5. After the final commit, the Phase 7 milestone is complete. Update `.planning/ROADMAP.md` Progress table to mark Phase 7 as complete with today's date — this is a small `docs(07): mark Phase 7 complete in roadmap` follow-up commit if desired (or fold into the SUMMARY commit).
  </action>
  <verify>
    <automated>test -f .planning/phases/07-banani-delta-polish/07-03-SUMMARY.md && grep -q "PLSH-06" .planning/phases/07-banani-delta-polish/07-03-SUMMARY.md && grep -q "PLSH-07" .planning/phases/07-banani-delta-polish/07-03-SUMMARY.md && grep -q "PLSH-08" .planning/phases/07-banani-delta-polish/07-03-SUMMARY.md</automated>
  </verify>
  <done>
    All builds + lint + ring purity + grep guards green. `git diff package.json` empty. SUMMARY exists with PLSH-06, PLSH-07, PLSH-08 all marked closed. Phase 7 milestone complete (PLSH-01 through PLSH-08 all closed across the three plans). One commit (SUMMARY) + optional roadmap update commit.
  </done>
</task>

</tasks>

<verification>
- `npm run lint` 0 new warnings
- `npm run build` 0 errors
- `cd backend && npm run build` 0 errors
- `git diff package.json` empty (zero new deps)
- `grep -rn "framer-motion" src/ package.json` returns nothing
- Ring 1 purity: `Calendar.tsx` and `Combobox.tsx` have NO `@/lib/api`, `@/lib/useApi`, `@/lib/constants`, or `@/contexts` imports
- No native `<select>` in `src/app/(authed)/tableau-de-bord/nouvelle/festive/etape-1`
- No `<input type="date">` in either wizard etape-2 page
- `thankYouMessage` present in `backend/src/lib/blocks/schemas.ts`
- Manual QA: all 8 checklist items pass
</verification>

<success_criteria>
- PLSH-06 closed: `<Calendar>` Ring 1 popover primitive in use on both wizard etape-2 pages.
- PLSH-07 closed: `<Combobox>` Ring 1 primitive in use on wizard festive étape 1 (and solidaire étape 1 if applicable).
- PLSH-08 closed: `thankYouMessage` Zod additive + wizard capture + creator edit + donor merci render — full round-trip.
- Zero new dependencies.
- Phase 7 milestone complete (PLSH-01 through PLSH-08 closed across 07-01/02/03).
- 8 atomic commits.
- SUMMARY.md exists.
</success_criteria>

<output>
After completion, create `.planning/phases/07-banani-delta-polish/07-03-SUMMARY.md` per template.
</output>
