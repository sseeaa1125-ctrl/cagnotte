---
phase: 07-banani-delta-polish
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/lib/constants.ts
  - src/components/ui/VisibilityCard.tsx
  - src/components/ui/index.ts
  - src/components/ui/DatePicker.tsx
  - src/app/(authed)/retraits/page.tsx
  - src/app/(authed)/retraits/pin/page.tsx
  - src/app/(authed)/retraits/succes/page.tsx
  - src/app/(authed)/profil/coordonnees-bancaires/page.tsx
  - src/app/(authed)/tableau-de-bord/nouvelle/festive/etape-3/page.tsx
  - src/app/(authed)/tableau-de-bord/nouvelle/solidaire/etape-3/page.tsx
autonomous: true
requirements: [PLSH-04, PLSH-05]
must_haves:
  truths:
    - "/retraits renders dark-navy hero, numbered pink-circle step markers, Wave/Orange operator tiles, uppercase RÉCAPITULATIF DU RETRAIT label, lock-icon footer."
    - "/retraits/pin renders shield hero, rounded-[2.5rem] outer card, 4-cell PIN grid with animate-pulse caret + focus-within:ring-blue-50 — persistent PIN, no countdown, no resend, no SMS OTP backend."
    - "/retraits/succes renders animate-ping green ring behind the solid check icon."
    - "/profil/coordonnees-bancaires renders Mobile Money card (Wave/Orange tiles + trash) + Bank Accounts dashed-border empty-state + footer security notice."
    - "VisibilityCard exists as a Ring 1 primitive in src/components/ui/VisibilityCard.tsx and is consumed by both wizard etape-3 pages."
    - "DatePicker wrapper is restyled to the Banani button shell (border-gray-300 rounded-xl px-4 py-3.5 + calendar icon)."
  artifacts:
    - path: "src/components/ui/VisibilityCard.tsx"
      provides: "Lifted shared visibility option card primitive (Ring 1 pure)"
      min_lines: 30
    - path: "src/components/ui/index.ts"
      provides: "Barrel export of VisibilityCard"
      contains: "VisibilityCard"
    - path: "src/app/(authed)/retraits/page.tsx"
      provides: "Restyled withdraw amount step matching WithdrawFundsForm.jsx"
      contains: "RÉCAPITULATIF"
    - path: "src/app/(authed)/retraits/pin/page.tsx"
      provides: "Visual restyle of persistent PIN entry (no countdown, no resend)"
      contains: "rounded-[2.5rem]"
    - path: "src/app/(authed)/retraits/succes/page.tsx"
      provides: "animate-ping success ring"
      contains: "animate-ping"
    - path: "src/app/(authed)/profil/coordonnees-bancaires/page.tsx"
      provides: "Full rewrite from UserPaymentMethods.jsx"
      min_lines: 100
  key_links:
    - from: "src/app/(authed)/tableau-de-bord/nouvelle/festive/etape-3/page.tsx"
      to: "src/components/ui/VisibilityCard.tsx"
      via: "import + render"
      pattern: "VisibilityCard"
    - from: "src/app/(authed)/tableau-de-bord/nouvelle/solidaire/etape-3/page.tsx"
      to: "src/components/ui/VisibilityCard.tsx"
      via: "import + render"
      pattern: "VisibilityCard"
---

<objective>
Phase 7, Plan 02 — P1 polish batch: visual parity for the withdraw flow, full rewrite of `/profil/coordonnees-bancaires`, lift `VisibilityCard` to a shared Ring 1 primitive, refresh wizard etape-3 copy, and restyle the `DatePicker` wrapper as a Banani button-shell.

Purpose: Every shipped page that has a visual mismatch against the Banani source gets restyled. NO new primitives are introduced in this plan (those land in 07-03). NO new backend surface, NO new dependencies, NO SMS OTP — the persistent 4-digit PIN that already ships stays exactly as is, only the chrome around it changes.

Output:
- `/retraits` matches `WithdrawFundsForm.jsx` chrome.
- `/retraits/pin` matches `WithdrawOTP.jsx` chrome (without the SMS countdown — persistent PIN only).
- `/retraits/succes` adds the `animate-ping` green ring.
- `/profil/coordonnees-bancaires` fully rewritten from `UserPaymentMethods.jsx`.
- `VisibilityCard` lifted to `src/components/ui/VisibilityCard.tsx`, both wizard etape-3 pages consume it, copy refreshed verbatim.
- `DatePicker` wrapper restyled to the Banani button-shell pattern (the underlying native `<input type="date">` stays functional; this is a temporary visual polish that 07-03 will supersede in wizard etape-2 by replacing the field with the new `<Calendar>` popover primitive).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-banani-delta-polish/07-RESEARCH.md
@.planning/banani/PHASE-7-DELTA.md
@.planning/banani/screens/phase-7/withdraw-funds-form.md
@.planning/banani/screens/phase-7/withdraw-otp.md
@.planning/banani/screens/phase-7/withdraw-success.md
@.planning/banani/screens/phase-7/user-payment-methods.md
@.planning/banani/screens/phase-7/visibility-card.md
@.planning/banani/screens/phase-7/solidaire-step-2-date-field.md
@.planning/phases/07-banani-delta-polish/07-01-SUMMARY.md
@CLAUDE.md
@src/lib/constants.ts
@src/components/ui/index.ts
@src/components/ui/RadioCard.tsx
@src/components/ui/Button.tsx
@src/components/ui/DatePicker.tsx
@src/app/(authed)/retraits/page.tsx
@src/app/(authed)/retraits/pin/page.tsx
@src/app/(authed)/retraits/succes/page.tsx
@src/app/(authed)/profil/coordonnees-bancaires/page.tsx
@src/app/(authed)/tableau-de-bord/nouvelle/festive/etape-3/page.tsx
@src/app/(authed)/tableau-de-bord/nouvelle/solidaire/etape-3/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 0: Constants block + lift VisibilityCard to a Ring 1 primitive</name>
  <files>src/lib/constants.ts, src/components/ui/VisibilityCard.tsx, src/components/ui/index.ts, src/app/(authed)/tableau-de-bord/nouvelle/festive/etape-3/page.tsx, src/app/(authed)/tableau-de-bord/nouvelle/solidaire/etape-3/page.tsx</files>
  <action>
    1. **Constants** (`src/lib/constants.ts`):
       Add new exported blocks (or extend existing ones). Use the verbatim Banani French copy from the wireframe files cited above.

       - `WITHDRAW_LABELS`:
         - `pageTitle: "Retirer mes fonds"`
         - `pageHelper: "Choisissez le montant à retirer et le compte de destination."`
         - `step1Title: "Montant à retirer"`
         - `step2Title: "Où envoyer l'argent ?"`
         - `addAccountCta: "Ajouter un compte bancaire ou Mobile Money"`
         - `summaryLabel: "RÉCAPITULATIF DU RETRAIT"`
         - `summaryAmount: "Montant"`
         - `summaryFees: "Frais"`
         - `summaryNet: "Net à recevoir"`
         - `confirmCta: "Confirmer le retrait"`
         - `securedFooter: "Transaction sécurisée"`

       - `WITHDRAW_PIN_LABELS`:
         - `pageTitle: "Vérification de sécurité"`
         - `pageHelper(amount: string): \`Pour valider votre retrait de ${amount}, veuillez saisir votre code PIN à 4 chiffres.\``
         - `submitCta: "Valider le retrait"`
         - `cancelCta: "Annuler"`
         - `pinError: "Code PIN incorrect."`

       - `WITHDRAW_SUCCESS_LABELS`:
         - `title: "Retrait confirmé !"`
         - `helper: "Votre retrait sera traité dans les prochaines minutes."`
         - `backCta: "Retour au tableau de bord"`

       - `BANK_ACCOUNTS_LABELS`:
         - `pageTitle: "Coordonnées bancaires"`
         - `mobileMoneyTitle: "Comptes Mobile Money"`
         - `mobileMoneyHelper: "Pour recevoir les fonds de vos cagnottes instantanément."`
         - `bankAccountsTitle: "Comptes Bancaires"`
         - `bankAccountsHelper: "Pour les virements bancaires classiques (délai de 48h à 72h)."`
         - `addAccountCta: "Ajouter"`
         - `addBankCta: "Ajouter un compte bancaire"`
         - `activeBadge: "Actif"`
         - `securityNoticeTitle: "Sécurité de vos coordonnées"`
         - `securityNoticeBody: "Vos coordonnées bancaires et numéros Mobile Money sont cryptés et stockés de manière sécurisée. Ils ne sont utilisés que pour procéder au virement des fonds récoltés sur vos cagnottes."`

       - `VISIBILITY_LABELS`:
         - `publicTitle: "Publique"`
         - `publicDescription: "Idéal pour maximiser les dons."`
         - `privateTitle: "Privée"`
         - `privateDescription: "Parfait pour les événements privés."` (verify against `.planning/banani/screens/phase-7/visibility-card.md` and use the verbatim string from there)

       Use JSX brace expressions in any consuming file for apostrophes, but the constants file is plain TS strings — apostrophes inside `"..."` strings need no escaping.

    2. **Lift VisibilityCard** to `src/components/ui/VisibilityCard.tsx`:
       - Open both `festive/etape-3/page.tsx` and `solidaire/etape-3/page.tsx`. Find the inline visibility option card markup (a card with an icon, title, description, and selected state). It is currently duplicated.
       - Create `src/components/ui/VisibilityCard.tsx` as a **Ring 1 pure** functional component. Props:
         ```ts
         interface VisibilityCardProps {
           value: string;          // the option's value identifier
           checked: boolean;       // is this card the currently selected option
           onChange: (value: string) => void;
           icon: React.ReactNode;  // lucide icon node passed in by parent
           title: string;
           description: string;
           name?: string;          // radio group name (default "visibility")
         }
         ```
       - Render: `<button type="button" role="radio" aria-checked={checked}>` styled exactly like the existing inline cards. Selected state uses `border-primary bg-accent` (or matching tokens). Touch target ≥ 48px (`min-h-[64px]` recommended).
       - **Ring 1 purity rules** (CRITICAL — verified by `bash scripts/verify-ring-purity.sh` if it exists):
         - NO imports from `@/lib/api`, `@/lib/useApi`, `@/contexts/*`, `@/lib/constants`.
         - Only pure props in / event out. The parent passes the title/description strings.
       - Add to `src/components/ui/index.ts` barrel export.

    3. **Update both wizard etape-3 pages** to import `VisibilityCard` from `@/components/ui` and the labels from `VISIBILITY_LABELS`. Replace the inline JSX with two `<VisibilityCard>` instances. Pass lucide icons (`Globe`, `Lock` or whatever the existing inline cards used) from the parent. Verify the existing form state binding (`config.visibility` likely) is preserved.

    4. Run `npm run build` and `bash scripts/verify-ring-purity.sh` (if present).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20 && grep -E "from \"@/lib/(api|useApi|constants)\"|from \"@/contexts" src/components/ui/VisibilityCard.tsx | head -5</automated>
  </verify>
  <done>
    Constants exported. `VisibilityCard.tsx` exists, Ring 1 pure (no domain imports), barrel exported. Both wizard etape-3 pages import and consume the lifted primitive with the new copy. Build green. Ring purity script (if present) passes. One commit: `refactor(07-02): lift VisibilityCard to ui/ + polish constants block`.
  </done>
</task>

<task type="auto">
  <name>Task 1: /retraits restructure (dark-navy hero + numbered steps + operator tiles)</name>
  <files>src/app/(authed)/retraits/page.tsx</files>
  <action>
    Reference: `.planning/banani/screens/phase-7/withdraw-funds-form.md` (mirrors `WithdrawFundsForm.jsx`).

    Rewrite the body of `src/app/(authed)/retraits/page.tsx` to match the Banani layout. Keep the underlying form state, validation, and submission logic — only the markup/styling changes. If the page is currently a server component with a client form island, keep that split.

    Layout (top to bottom):

    1. **Dark-navy hero header** — full-bleed `bg-primary text-white` (Tailwind `bg-primary` mapped to navy `#172866`):
       ```tsx
       <header className="bg-primary text-white px-6 py-8 rounded-b-3xl">
         <h1 className="font-poppins font-black text-2xl">{WITHDRAW_LABELS.pageTitle}</h1>
         <p className="text-sm text-white/80 mt-2">{WITHDRAW_LABELS.pageHelper}</p>
       </header>
       ```

    2. **Step 1 — Montant à retirer**:
       - Numbered marker: `<div className="w-8 h-8 rounded-full bg-accent text-primary font-bold flex items-center justify-center">1</div>` next to `<h2>{WITHDRAW_LABELS.step1Title}</h2>`.
       - Amount input with FCFA suffix, formatted via `formatPrice` on blur (or a controlled formatter). Reuse the existing `<Input>` primitive.

    3. **Step 2 — Où envoyer l'argent ?**:
       - Numbered marker `2` (same pattern as step 1).
       - Two operator tile buttons (Wave + Orange Money). Each tile:
         ```tsx
         <button className="border border-gray-200 hover:border-primary rounded-2xl p-4 flex items-center gap-3 transition-colors">
           <span className="w-12 h-12 rounded-xl bg-[#3374FF] text-white font-bold flex items-center justify-center">W</span>
           <div className="text-left">
             <p className="font-bold text-primary">Wave</p>
             <p className="text-sm text-gray-500">{maskedPhone}</p>
           </div>
         </button>
         ```
         Orange variant uses `bg-[#FF6600]` and the letter `O`.
       - Below the tiles, the dashed-border "Ajouter un compte" CTA:
         ```tsx
         <Link href="/profil/coordonnees-bancaires" className="border-2 border-dashed border-gray-300 rounded-2xl p-4 flex items-center justify-center text-gray-500 hover:border-primary hover:text-primary transition-colors">
           + {WITHDRAW_LABELS.addAccountCta}
         </Link>
         ```

    4. **Summary section**:
       ```tsx
       <p className="uppercase tracking-wider text-xs text-gray-500 font-bold">{WITHDRAW_LABELS.summaryLabel}</p>
       ```
       Followed by a card with three rows: Montant / Frais / Net à recevoir, all formatted via `formatPrice`.

    5. **Confirm CTA** — primary `<Button>`: `{WITHDRAW_LABELS.confirmCta}`. On click → existing PIN gate flow (route to `/retraits/pin?amount=...&blockId=...`). Use `router.replace` if appropriate; the existing implementation pattern likely already uses router.push — match the existing convention.

    6. **Footer** — small centered `<p>` with `<Lock>` icon (lucide) + `{WITHDRAW_LABELS.securedFooter}`:
       ```tsx
       <p className="flex items-center justify-center gap-2 text-gray-500 text-sm mt-6">
         <Lock className="w-4 h-4" /> {WITHDRAW_LABELS.securedFooter}
       </p>
       ```

    Constraints:
    - Mobile-first 375px. Touch targets ≥ 48px.
    - All copy from constants (zero hardcoded French in JSX).
    - No new dependencies. Use `lucide-react` already in the project.
    - Use `bg-accent` Tailwind utility for the pink-100 numbered marker (token mapped to `#FBE6ED`).
    - `formatPrice` from `@/lib/utils`.
    - JSX brace expressions for apostrophes.
  </action>
  <verify>
    <automated>npm run lint -- src/app/\(authed\)/retraits/page.tsx 2>&1 | tail -10 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    `/retraits` renders the dark-navy hero, numbered pink step markers, Wave/Orange operator tiles with colored-initial squares, dashed-border "Ajouter un compte" CTA, uppercase summary label, lock-icon footer. Existing form logic and submission preserved. Lint + build green. One commit: `feat(07-02): restyle /retraits to Banani WithdrawFundsForm`.
  </done>
</task>

<task type="auto">
  <name>Task 2: /retraits/pin visual restyle (persistent PIN, no countdown)</name>
  <files>src/app/(authed)/retraits/pin/page.tsx</files>
  <action>
    Reference: `.planning/banani/screens/phase-7/withdraw-otp.md` (mirrors `WithdrawOTP.jsx`).

    **CRITICAL**: This is a VISUAL restyle ONLY. The underlying `Seller.withdrawalPinHash` persistent 4-digit PIN flow stays exactly as shipped — NO countdown, NO resend, NO SMS OTP backend, NO new endpoint.

    Rewrite the markup of `src/app/(authed)/retraits/pin/page.tsx` (keep all state + submission logic):

    1. **Outer container** — `max-w-lg mx-auto bg-white rounded-[2.5rem] p-8 md:p-10 shadow-sm` (the verbatim 2.5rem corner radius is a Banani signature).

    2. **Shield hero**:
       ```tsx
       <div className="flex justify-center mb-6">
         <div className="bg-blue-50 rounded-full w-20 h-20 flex items-center justify-center">
           <ShieldCheck className="w-10 h-10 text-primary" />
         </div>
       </div>
       ```
       Import `ShieldCheck` from `lucide-react`.

    3. **Heading**:
       ```tsx
       <h1 className="font-poppins font-black text-2xl text-center text-primary">
         {WITHDRAW_PIN_LABELS.pageTitle}
       </h1>
       <p className="text-center text-gray-500 mt-2">
         {WITHDRAW_PIN_LABELS.pageHelper(formatPrice(amount))}
       </p>
       ```
       The `pageHelper` is a function constant — call it with the formatted amount.

       **Do NOT include any phone-masked text** ("envoyé au +221 ..."). This is persistent PIN, not SMS OTP.

    4. **4-cell PIN grid**:
       ```tsx
       <div className="flex justify-center gap-3 mt-8">
         {[0,1,2,3].map(i => (
           <input
             key={i}
             ref={el => inputsRef.current[i] = el}
             type="text"
             inputMode="numeric"
             pattern="[0-9]*"
             maxLength={1}
             value={digits[i] || ""}
             onChange={e => handleChange(i, e.target.value)}
             onKeyDown={e => handleKeyDown(i, e)}
             className="w-14 h-16 rounded-2xl border-2 border-gray-200 text-center text-2xl font-bold text-primary focus-within:border-primary focus:border-primary focus:ring-4 focus:ring-blue-50 transition-all"
           />
         ))}
       </div>
       ```
       Reuse the 4-cell input pattern that already ships in `/verification-email` if practical (extract a small helper or inline copy — do NOT lift to a primitive in this plan; that is 07-03 territory if needed).

    5. **animate-pulse caret** on the currently-focused empty cell — implement by toggling a `bg-primary/40` 1px-wide pseudo-caret via a CSS class or by adding `animate-pulse` to the empty cell's border. Acceptable minimum: add `animate-pulse` to `border-primary` when the cell index === activeIndex && empty.

    6. **NO resend button. NO countdown timer.** Just the two CTAs:
       ```tsx
       <Button variant="primary" disabled={pin.length !== 4 || submitting}>
         {WITHDRAW_PIN_LABELS.submitCta}
       </Button>
       <Button variant="ghost" onClick={() => router.back()}>
         {WITHDRAW_PIN_LABELS.cancelCta}
       </Button>
       ```

    7. Error display — on incorrect PIN, show `{WITHDRAW_PIN_LABELS.pinError}` in a red text block above the grid. Reuse the existing toast system if the page already uses it.

    8. Existing submission logic stays: POST to whatever the existing PIN verify endpoint is (likely `/api/withdrawals` with `pin` in body, or a dedicated `/api/sellers/withdrawal-pin/verify` — preserve whatever ships).

    Constraints:
    - JSX brace expressions for apostrophes.
    - All copy from `WITHDRAW_PIN_LABELS`.
    - No new dependencies.
    - Mobile-first.
    - DO NOT add any setTimeout / countdown logic. DO NOT add a "Renvoyer le code" link.
  </action>
  <verify>
    <automated>npm run lint -- src/app/\(authed\)/retraits/pin/page.tsx 2>&1 | tail -10 && grep -n "rounded-\[2.5rem\]\|ShieldCheck\|focus:ring-blue-50" src/app/\(authed\)/retraits/pin/page.tsx && ! grep -n "setTimeout.*[0-9].*1000\|Renvoyer\|countdown\|otpResend" src/app/\(authed\)/retraits/pin/page.tsx  <verify>
    <automated>npm run lint -- src/app/\(authed\)/retraits/pin/page.tsx 2>&1 | tail -10 && grep -n "rounded-\[2.5rem\]" src/app/\(authed\)/retraits/pin/page.tsx</automated>
  </verify>
  <done>
    `/retraits/pin` renders shield hero in `bg-blue-50 rounded-full w-20 h-20`, outer `rounded-[2.5rem]` card, 4-cell PIN grid with `focus:ring-blue-50` + `animate-pulse` caret on the active empty cell, NO countdown, NO resend, NO phone-mask text. Existing PIN verification logic preserved. Lint + grep guards green. One commit: `feat(07-02): restyle /retraits/pin to Banani WithdrawOTP chrome (persistent PIN, no countdown)`.
  </done>
</task>

<task type="auto">
  <name>Task 3: /retraits/succes animate-ping green ring</name>
  <files>src/app/(authed)/retraits/succes/page.tsx</files>
  <action>
    Reference: `.planning/banani/screens/phase-7/withdraw-success.md` (mirrors `WithdrawSuccess.jsx`).

    Update `src/app/(authed)/retraits/succes/page.tsx` so the success check icon is wrapped in an `animate-ping` ring identical to the WithdrawSuccess pattern.

    1. Replace the existing check icon block with:
       ```tsx
       <div className="relative w-24 h-24 mx-auto">
         <div className="absolute inset-0 rounded-full bg-[#00B67A]/20 animate-ping"/>
         <div className="absolute inset-0 rounded-full bg-[#E6F3EE] flex items-center justify-center">
           <Check className="w-12 h-12 text-[#00B67A]" strokeWidth={3}/>
         </div>
       </div>
       ```
       Import `Check` from `lucide-react`.

    2. Below the icon, render the title + helper from `WITHDRAW_SUCCESS_LABELS`:
       ```tsx
       <h1 className="font-poppins font-black text-2xl text-center text-primary mt-6">
         {WITHDRAW_SUCCESS_LABELS.title}
       </h1>
       <p className="text-center text-gray-500 mt-2">
         {WITHDRAW_SUCCESS_LABELS.helper}
       </p>
       ```

    3. CTA back to dashboard:
       ```tsx
       <Button variant="primary" onClick={() => router.replace("/tableau-de-bord")}>
         {WITHDRAW_SUCCESS_LABELS.backCta}
       </Button>
       ```
       Use `router.replace` so the back button does not return to the PIN page.

    4. Mobile-first, centered layout. No new dependencies.
  </action>
  <verify>
    <automated>npm run lint -- src/app/\(authed\)/retraits/succes/page.tsx 2>&1 | tail -10 && grep -n "animate-ping" src/app/\(authed\)/retraits/succes/page.tsx</automated>
  </verify>
  <done>
    `animate-ping` green ring renders behind the solid check icon. Title + helper + dashboard CTA all sourced from constants. `router.replace` used. Lint green. One commit: `feat(07-02): animate-ping green ring on /retraits/succes`.
  </done>
</task>

<task type="auto">
  <name>Task 4: /profil/coordonnees-bancaires full rewrite from UserPaymentMethods.jsx</name>
  <files>src/app/(authed)/profil/coordonnees-bancaires/page.tsx</files>
  <action>
    Reference: `.planning/banani/screens/phase-7/user-payment-methods.md` (mirrors `UserPaymentMethods.jsx`).

    **Full rewrite** of `src/app/(authed)/profil/coordonnees-bancaires/page.tsx`. Keep the file location (so the existing route still resolves), keep the `ProfileSidebar` integration with `activeTab="bank"`, preserve any data-fetching wired in Phase 6 (the page likely already calls `GET /api/sellers/me` to read `payoutPhone`/`payoutProvider`/`payoutName`).

    Layout:

    1. Header row: `<h1>{BANK_ACCOUNTS_LABELS.pageTitle}</h1>`.

    2. **Card 1 — Comptes Mobile Money**:
       ```tsx
       <section className="bg-white rounded-2xl border border-gray-200 p-6">
         <div className="flex items-center justify-between mb-2">
           <h2 className="font-poppins font-bold text-lg text-primary">
             {BANK_ACCOUNTS_LABELS.mobileMoneyTitle}
           </h2>
           <Button variant="outline" size="sm">{BANK_ACCOUNTS_LABELS.addAccountCta}</Button>
         </div>
         <p className="text-sm text-gray-500 mb-4">{BANK_ACCOUNTS_LABELS.mobileMoneyHelper}</p>

         {/* Account rows */}
         {accounts.map(account => (
           <div className="flex items-center gap-3 py-3 border-t border-gray-100">
             <span className={`w-12 h-12 rounded-xl text-white font-bold flex items-center justify-center ${account.provider === "wave" ? "bg-[#3374FF]" : "bg-[#FF6600]"}`}>
               {account.provider === "wave" ? "W" : "O"}
             </span>
             <div className="flex-1">
               <p className="font-bold text-primary">{account.provider === "wave" ? "Wave" : "Orange Money"}</p>
               <p className="text-sm text-gray-500">{formatPhone(account.phone)}</p>
             </div>
             <span className="bg-green-50 text-green-700 text-xs font-bold px-3 py-1 rounded-full">
               {BANK_ACCOUNTS_LABELS.activeBadge}
             </span>
             <button aria-label="Supprimer" className="text-gray-400 hover:text-red-600 transition-colors">
               <Trash2 className="w-5 h-5"/>
             </button>
           </div>
         ))}
       </section>
       ```
       Empty state if no accounts: an inline helper "Aucun compte Mobile Money configuré." with the "Ajouter" CTA prominent.

       **Wave + Orange ONLY** — Free Money is NOT supported (D-22 still applies). Filter the provider list accordingly.

    3. **Card 2 — Comptes Bancaires** (empty-state only in v1):
       ```tsx
       <section className="bg-white rounded-2xl border border-gray-200 p-6">
         <h2 className="font-poppins font-bold text-lg text-primary">
           {BANK_ACCOUNTS_LABELS.bankAccountsTitle}
         </h2>
         <p className="text-sm text-gray-500 mb-4">{BANK_ACCOUNTS_LABELS.bankAccountsHelper}</p>

         <button className="w-full border-2 border-dashed border-gray-300 rounded-2xl p-6 flex flex-col items-center justify-center gap-2 text-gray-500 hover:border-primary hover:text-primary transition-colors">
           <Landmark className="w-8 h-8"/>
           <span className="font-bold">{BANK_ACCOUNTS_LABELS.addBankCta}</span>
         </button>
       </section>
       ```
       The button is a no-op stub for v1 (or routes to a "coming soon" toast). Add a TODO comment: `// TODO PHASE-8: bank account add flow`.

    4. **Footer security notice**:
       ```tsx
       <aside className="bg-blue-50 rounded-2xl p-5 mt-6">
         <p className="font-bold text-primary mb-1">{BANK_ACCOUNTS_LABELS.securityNoticeTitle}</p>
         <p className="text-sm text-gray-600">{BANK_ACCOUNTS_LABELS.securityNoticeBody}</p>
       </aside>
       ```

    5. Imports: `Trash2`, `Landmark` from `lucide-react`. `formatPhone` from `@/lib/utils`. `BANK_ACCOUNTS_LABELS` from `@/lib/constants`. `Button` + `ProfileSidebar` from existing locations.

    6. Mobile-first, two cards stack on mobile, side-by-side or stacked on desktop within the sidebar layout. Touch targets ≥ 48px. Zero hardcoded French.

    7. The "Ajouter" buttons (mobile money + bank) currently route to no-op or a stub. Do NOT add a real add-account modal in this plan.
  </action>
  <verify>
    <automated>npm run lint -- src/app/\(authed\)/profil/coordonnees-bancaires/page.tsx 2>&1 | tail -10 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    `/profil/coordonnees-bancaires` renders the two-card layout (Mobile Money + Bank Accounts empty state) + footer security notice. Wave/Orange tiles use the brand colors. Trash icon row works. ProfileSidebar still active. Lint + build green. One commit: `feat(07-02): rewrite /profil/coordonnees-bancaires from UserPaymentMethods.jsx`.
  </done>
</task>

<task type="auto">
  <name>Task 5: Wizard etape-3 copy refresh + VisibilityCard integration verification</name>
  <files>src/app/(authed)/tableau-de-bord/nouvelle/festive/etape-3/page.tsx, src/app/(authed)/tableau-de-bord/nouvelle/solidaire/etape-3/page.tsx</files>
  <action>
    Note: Task 0 already lifted `VisibilityCard` and updated both etape-3 pages to consume it. This task is the **copy verification + apostrophe sweep**.

    1. Open both files and confirm the imports + render of `VisibilityCard` from `@/components/ui` are wired correctly.

    2. Verify the `VISIBILITY_LABELS.publicDescription` and `VISIBILITY_LABELS.privateDescription` strings are the verbatim Banani copy from `.planning/banani/screens/phase-7/visibility-card.md`. If the wireframe uses a slightly different French phrase than what Task 0 added, update the constants file.

    3. Sweep both files for any remaining inline French strings. Move them to constants.

    4. JSX brace expressions for apostrophes (e.g., `{"l'événement"}`).

    5. Verify the existing form state binding for `config.visibility` is preserved after the migration to `VisibilityCard`. Both options ("public" and "private") must round-trip into the wizard sessionStorage draft.

    6. If the etape-3 page already had the "submit / create cagnotte" CTA, leave it untouched.
  </action>
  <verify>
    <automated>npm run lint -- src/app/\(authed\)/tableau-de-bord/nouvelle/festive/etape-3/page.tsx src/app/\(authed\)/tableau-de-bord/nouvelle/solidaire/etape-3/page.tsx 2>&1 | tail -10 && grep -n "VisibilityCard" src/app/\(authed\)/tableau-de-bord/nouvelle/festive/etape-3/page.tsx src/app/\(authed\)/tableau-de-bord/nouvelle/solidaire/etape-3/page.tsx</automated>
  </verify>
  <done>
    Both wizard etape-3 pages import + render `VisibilityCard` from `@/components/ui`. Copy matches Banani verbatim. Form state preserved. Lint green. One commit: `chore(07-02): wizard etape-3 copy + apostrophe sweep`.
  </done>
</task>

<task type="auto">
  <name>Task 6: DatePicker wrapper restyle to Banani button-shell</name>
  <files>src/components/ui/DatePicker.tsx</files>
  <action>
    Reference: `.planning/banani/screens/phase-7/solidaire-step-2-date-field.md`.

    Restyle the `DatePicker` primitive to look like the Banani button shell. Keep the underlying functional `<input type="date">` for native date picking — this is a wrapper restyle only.

    1. Open `src/components/ui/DatePicker.tsx`.

    2. Wrap the existing `<input type="date">` in a styled "button shell" container:
       ```tsx
       <div className="relative">
         <div className="border border-gray-300 rounded-xl px-4 py-3.5 bg-white text-gray-400 flex justify-between items-center cursor-pointer hover:border-primary transition-colors shadow-sm">
           <span className={value ? "text-primary font-medium" : ""}>
             {value ? formatDateDisplay(value) : (placeholder ?? "Sélectionnez une date...")}
           </span>
           <Calendar className="w-5 h-5 text-gray-400"/>
         </div>
         <input
           type="date"
           value={value ?? ""}
           onChange={onChange}
           className="absolute inset-0 opacity-0 cursor-pointer"
           min={minDate}
           max={maxDate}
         />
       </div>
       ```
       The native input is absolutely positioned + transparent so a click anywhere on the styled shell opens the native picker.

    3. Import `Calendar` from `lucide-react`.

    4. Provide a `formatDateDisplay(iso: string)` local helper (or reuse from `@/lib/utils` if it exists) returning a French-formatted date like `"15 avril 2026"`.

    5. **Ring 1 purity** — the file MUST NOT import `@/lib/api`, `@/lib/useApi`, `@/contexts/*`, or `@/lib/constants`. The placeholder is passed as a prop with a hardcoded fallback.

    6. **NOTE**: This is a TEMPORARY wrapper restyle. Plan 07-03 will REPLACE this usage in wizard etape-2 with the new `<Calendar>` popover primitive. This restyle benefits any other DatePicker usage. Run `grep -rn "DatePicker" src/` to enumerate all current usages — if etape-2 is the only consumer, that is fine; the restyle still ships as a baseline visual.

    7. Run the ring purity script if it exists: `bash scripts/verify-ring-purity.sh`.
  </action>
  <verify>
    <automated>npm run lint -- src/components/ui/DatePicker.tsx 2>&1 | tail -10 && grep -E "from \"@/lib/(api|useApi|constants)\"|from \"@/contexts" src/components/ui/DatePicker.tsx | head -5 && grep -n "rounded-xl px-4 py-3.5" src/components/ui/DatePicker.tsx</automated>
  </verify>
  <done>
    `DatePicker.tsx` renders the Banani button shell with `border-gray-300 rounded-xl px-4 py-3.5 bg-white` and trailing `Calendar` icon. Native input still functional under an absolute overlay. Ring 1 purity preserved (zero domain imports). Lint green. One commit: `feat(07-02): restyle DatePicker wrapper to Banani button shell`.
  </done>
</task>

<task type="auto">
  <name>Task 7: Final verification + 07-02-SUMMARY.md</name>
  <files>.planning/phases/07-banani-delta-polish/07-02-SUMMARY.md</files>
  <action>
    1. Run the full verification battery:
       ```
       npm run lint
       npm run build
       cd backend && npm run build && cd ..
       bash scripts/verify-ring-purity.sh   # if present
       git diff package.json                # MUST be empty (zero new deps)
       grep -rn "framer-motion" src/        # must return nothing
       grep -rn "VisibilityCard" src/components/ui src/app
       grep -rn "rounded-\[2.5rem\]" src/app/\(authed\)/retraits/pin/
       grep -rn "animate-ping" src/app/\(authed\)/retraits/succes/
       ```

    2. Manual QA checklist (run dev server in two terminals per CLAUDE.md):
       - Visit `/retraits` → dark-navy hero, numbered pink markers, Wave/Orange tiles render.
       - Click "Confirmer le retrait" → lands on `/retraits/pin` → shield hero, 4-cell PIN, NO countdown text, NO "Renvoyer" link.
       - Submit valid PIN → lands on `/retraits/succes` → see green `animate-ping` ring.
       - Visit `/profil/coordonnees-bancaires` → two cards (Mobile Money with Wave/Orange tiles + Bank Accounts dashed empty state) + footer notice.
       - Open festive wizard étape 3 → see two `VisibilityCard` options, confirm copy is verbatim Banani.

    3. Create `.planning/phases/07-banani-delta-polish/07-02-SUMMARY.md` per template. Include:
       - **Requirements closed**: PLSH-04, PLSH-05 (each with one-line evidence)
       - **Files created** (1: `VisibilityCard.tsx`) and **Files modified** (9)
       - **Commits** (8 atomic conventional commits, one per task — T0 refactor + T1/T2/T3/T4 feat + T5 chore + T6 feat + T7 docs)
       - **Verification results** (lint / build / package.json diff empty / grep guards)
       - **Risks / follow-ups for 07-03**: `DatePicker` wrapper restyle is the visual baseline that the new `<Calendar>` popover (07-03) will replace in wizard etape-2; the share button on the creator detail page (from 07-01) remains a stub.

    4. Commit the SUMMARY: `docs(07-02): SUMMARY for P1 polish batch`.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-banani-delta-polish/07-02-SUMMARY.md && grep -q "PLSH-04" .planning/phases/07-banani-delta-polish/07-02-SUMMARY.md && grep -q "PLSH-05" .planning/phases/07-banani-delta-polish/07-02-SUMMARY.md</automated>
  </verify>
  <done>
    All builds + lint + ring purity + grep guards green. `git diff package.json` empty (zero new deps). SUMMARY file exists with PLSH-04 and PLSH-05 marked closed. One commit: `docs(07-02): SUMMARY for P1 polish batch`.
  </done>
</task>

</tasks>

<verification>
- `npm run lint` 0 new warnings
- `npm run build` 0 errors
- `cd backend && npm run build` 0 errors
- `git diff package.json` empty (zero new deps)
- `grep -rn "framer-motion" src/` returns nothing
- Ring 1 purity script (if present) green — `VisibilityCard.tsx` and `DatePicker.tsx` have NO `@/lib/api`, `@/lib/useApi`, `@/lib/constants`, or `@/contexts` imports
- Manual QA: withdraw flow visual parity verified end-to-end on 375px viewport
- Manual QA: `/retraits/pin` has NO countdown, NO resend link, NO phone-mask text
- Manual QA: `/retraits/succes` shows `animate-ping` green ring
- Manual QA: `/profil/coordonnees-bancaires` matches `UserPaymentMethods.jsx` layout
- Manual QA: both wizard etape-3 pages render the lifted `VisibilityCard`
</verification>

<success_criteria>
- PLSH-04 closed: `/retraits` + `/retraits/pin` + `/retraits/succes` all match Banani chrome (persistent PIN, no SMS OTP, no countdown).
- PLSH-05 closed: `/profil/coordonnees-bancaires` rewritten from `UserPaymentMethods.jsx`.
- `VisibilityCard` is a Ring 1 primitive in `src/components/ui/`, consumed by both wizard etape-3 pages.
- `DatePicker` wrapper restyled to Banani button shell.
- 8 atomic commits.
- SUMMARY.md exists.
</success_criteria>

<output>
After completion, create `.planning/phases/07-banani-delta-polish/07-02-SUMMARY.md` per template.
</output>
